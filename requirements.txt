# Core dependencies
selenium>=4.0.0          # Browser automation
beautifulsoup4>=4.10.0   # HTML parsing and scraping
requests>=2.25.0         # HTTP requests handling
lxml>=4.6.0              # XML and HTML parsing (faster for BeautifulSoup)

# WebDriver management
webdriver-manager>=4.0.0  # Auto-manage ChromeDriver versions

#How to Use:
Just save this file as requirements.txt in your project directory and run:
 ```bash
pip install -r requirements.txt

```
